# KVM Dashboard — 项目状态同步文档 (Session Handover)

> 更新时间：2026-03-02  
> 当前分支：`feat/full-refactor`  
> 本文档为 AI 工作 session 之间的完整交接凭证。

---

## 一、项目概述

上海机场 G&D KVM 设备（ControlCenter-Compact / ControlCenter-IP）的工业级实时监控大屏系统。支持 SNMP 主动轮询 + 被动 Trap 接收双通道，WebSocket 毫秒级推送。

---

## 二、当前完成状态

### ✅ 已完成（100%）

| 模块 | 状态 | 说明 |
|---|---|---|
| **后端 API** | ✅ | FastAPI + SQLAlchemy 2.0，SNMP Poller 重构完成 |
| **数据库** | ✅ | SQLite 测试 + PostgreSQL/TimescaleDB 生产双模式 |
| **SNMP 通信** | ✅ | pysnmp v6 兼容、引擎池复用、并发限流、告警去重 |
| **前端大屏** | ✅ | 完整 Dashboard（KPI + 矩阵 + 拓扑 + 告警 + 图表） |
| **用户认证** | ✅ | JWT + Zustand persist + axios 拦截器 |
| **WebSocket** | ✅ | 实时推送 device_update / new_alerts / trap_received |

### ⏳ 待完成

| 模块 | 优先级 | 详细方案位置 |
|---|---|---|
| **Docker 部署** | P1 | `help/Refactor Plan` Phase 3 |

### 🎉 新增完成（2026-03-02）

| 模块 | 状态 | 说明 |
|---|---|---|
| **Admin 管理面板** | ✅ | 包含设备管理、别名配置、OID启停、告警处理、用户管理 5 个大 Tab，包含中英切换和暗色主题。 |
| **别名与展示集成** | ✅ | 别名系统已全链路打通（包含拓扑图和矩阵），并且 OID 开关可实时控制 Dashboard 显隐。 |
| **全站国际化** | ✅ | 彻底移除了 Hardcode 中文，实现中英双语无缝切换。 |

---

## 三、技术栈（与代码 100% 同步）

### 后端
| 组件 | 技术 |
|---|---|
| Web 框架 | FastAPI (Python 3.10) |
| 数据库 | PostgreSQL 16 + TimescaleDB / SQLite（测试） |
| ORM | SQLAlchemy 2.0 async + asyncpg/aiosqlite |
| SNMP | pysnmp (纯 asyncio) |
| 调度器 | APScheduler |
| 实时推送 | WebSocket (FastAPI 内置) |
| 认证 | JWT (python-jose + bcrypt==4.0.1) |

### 前端
| 组件 | 技术 | 说明 |
|---|---|---|
| 框架 | React 18 + Vite | |
| 状态 | Zustand | mainStore + authStore |
| 拓扑图 | **纯 SVG + DOM 手写渲染** | ⚠️ 不依赖 React Flow / D3.js |
| 图表 | **纯 SVG 手绘** | ⚠️ 不依赖 ECharts |
| 路由 | React Router v6 | /login → /dashboard → /admin |
| 样式 | Vanilla CSS + CSS Variables | 暗色大屏主题 |
| HTTP | axios + 拦截器 | JWT 自动注入 + 401 跳转 |
| WS | react-use-websocket | ws://host/api/v1/ws/monitor |

---

## 四、核心文件结构

```
frontend/src/
├── App.jsx                    # 路由定义 (Login / Dashboard / Admin Coming Soon)
├── pages/
│   ├── Login.jsx              # 登录页（粒子动效）
│   ├── Dashboard.jsx          # 大屏主页（KPI计算、布局编排）
│   └── Dashboard.css          # 大屏样式
├── components/
│   ├── DeviceCard.jsx         # 左栏 KVM 设备卡片
│   ├── MatrixView.jsx         # 终端状态矩阵（点击弹出 EndpointDetail）
│   ├── TopoView.jsx           # 设备拓扑图（纯 SVG+DOM，含缩放/平移/粒子）
│   ├── EndpointDetail.jsx     # 终端详情面板（矩阵+拓扑共享）
│   ├── AlertStream.jsx        # 右栏实时告警流
│   ├── BottomCharts.jsx       # 底栏图表（温度+风扇+在线率，纯 SVG）
│   ├── ProtectedRoute.jsx     # 路由守卫
│   ├── DeviceMatrix.jsx       # (旧) 设备热力矩阵，暂未使用
│   ├── EndpointGrid.jsx       # (旧) 终端网格，暂未使用
│   ├── HealthRate.jsx         # (旧) 健康率组件，已嵌入 Dashboard
│   ├── StatsBar.jsx           # (旧) 统计栏，已嵌入 Dashboard
│   ├── MetricChart.jsx        # (旧) ECharts 图表，已被 BottomCharts 替代
│   ├── TopologyView.jsx       # (旧) React Flow 拓扑，已被 TopoView 替代
│   └── EndpointNode.jsx       # (旧) React Flow 节点，已弃用
├── store/
│   ├── mainStore.js           # 全局状态（devices/endpoints/alerts/stats/topology）
│   └── authStore.js           # 认证状态（token/user/logout）
├── hooks/
│   └── useSystemWebSocket.js  # WebSocket 钩子
├── utils/
│   └── api.js                 # axios 实例 + 拦截器
└── i18n/                      # 多语言框架（预留）
```

---

## 五、关键设计决策记录

### 5.1 KPI 指标维度（2026-03-02 变更）

**变更前**：顶栏显示交换机台数（总设备/在线/离线），使用 `useCountUp` 动画。  
**变更后**：顶栏显示**终端**数量（总终端/活跃/离线），直接展示数值。

**原因**：
- 交换机一般不会离线（3台永远显示3/3），监控价值低
- `useCountUp` 在 WebSocket 每次推送时都会从 0 开始滚动，视觉体验差
- 终端维度（60台规模）能更真实反映系统健康状况

**计算口径**：
- 活跃 = `ep_device_status === 'online' || 'ready'`
- 离线 = `!ep_device_status || ep_device_status === 'offline'`
- 健康率 = `活跃 / 总终端 × 100`

### 5.2 矩阵交互方式（2026-03-02 变更）

**变更前**：hover 显示 tooltip（`.m-tooltip`）  
**变更后**：点击弹出 `EndpointDetail` 面板

**原因**：`.matrix-wrap` 的 `overflow` 裁剪导致边缘 tooltip 被截断遮挡，且 hover 在大量方块密集排列时容易误触。

### 5.3 拓扑图技术选型

**最终方案**：纯 SVG + DOM 手写渲染（`TopoView.jsx`）  
**弃用方案**：React Flow (`@xyflow/react`)

**原因**：React Flow 在频繁数据更新和 WebSocket 推送场景下性能不佳，且自定义流光粒子动画困难。纯 SVG 方案更轻量、可控性更强。

### 5.4 底栏图表技术选型

**最终方案**：纯 SVG 手绘（`BottomCharts.jsx`）  
**弃用方案**：ECharts (`echarts-for-react`)

**原因**：大屏场景下 ECharts 默认样式与暗色主题不协调，定制成本高。纯 SVG 更轻量且完全可控。

---

## 六、已修复的关键 Bug（2026-03-02 session）

| Bug | 根因 | 修复方式 |
|---|---|---|
| KPI 数字每次推送从 0 闪烁 | `useCountUp` 动画重置 | 移除 useCountUp，直接展示数值 |
| 矩阵 tooltip 被遮挡 | overflow 裁剪 | 改为点击弹出 EndpointDetail |
| 拓扑终端计数为 0 | 仅统计 `online`，遗漏 `ready` | 加入 `ready` 状态 |
| 告警时间戳缺失 | Trap 推送无 `created_at` | WS hook 添加时间戳兜底 |
| 健康率始终 0% | `stats` API 返回 0 | 改用前端 endpoints 计算 |
| 24H 在线率 NaN% | 除零错误 | 安全除法保护 |
| KPI 显示交换机台数（3） | 数据源错误 | 改为终端维度计算 |

---

## 七、下一步工作指引

### 优先级 P0：Admin 管理面板

详细方案：`tasks/frontend_plan.md` §3.3 + §10

需要新增的文件：
- `pages/Admin.jsx` + `Admin.css`
- `components/admin/DevicesTab.jsx`
- `components/admin/AliasesTab.jsx`
- `components/admin/OIDsTab.jsx`
- `components/admin/AlertsTab.jsx`
- `components/admin/UsersTab.jsx`

需要修改的文件：
- `App.jsx`：替换 `/admin` 路由占位
- `ProtectedRoute.jsx`：添加 admin 角色检查

### 优先级 P1：Docker 部署

需要新增：
- `frontend/Dockerfile`（多阶段构建 Node.js + Nginx）
- `frontend/nginx.conf`（SPA fallback + 反代 /api /ws）
- `docker-compose.yml`（3 容器编排）
- `docs/DEPLOYMENT.md`（交付手册）

---

## 八、核心文档索引

| 文档 | 路径 | 用途 |
|---|---|---|
| API 契约 | `tasks/api_docs.md` | 前端 axios 请求参考 |
| 前端设计方案 | `tasks/frontend_plan.md` | UI 设计、组件规划、Admin 面板 |
| 数据库结构 | `backend/init.sql` | PostgreSQL + TimescaleDB 建表 |
| 经验教训 | `tasks/lessons.md` | pysnmp、并发、DB 兼容性等避坑 |
| 进度追踪 | `tasks/todo.md` | 任务勾选及回顾日志 |
| 工作交接 | `docs/HANDOVER.md` | 系统启动指南 + 三大坑预警 |
| 指标说明 | `docs/metrics_reference.md` | 大屏每个数字的来源和计算逻辑 |
| 重构方案 | `help/Refactor Plan` | 架构设计和数据库变更说明 |

---

## 九、启动指南（快速复现）

```bash
# 终端 1：启动后端
cd backend
.venv\Scripts\python -m uvicorn app.main:app --host 0.0.0.0 --port 8000

# 终端 2：启动 SNMP 模拟器（3台交换机 × 20终端）
cd backend
.venv\Scripts\python run_simulators_large.py

# 终端 3：启动前端
cd frontend
npm run dev -- --force
```

**访问**：http://localhost:3000  
**登录**：admin / admin123
